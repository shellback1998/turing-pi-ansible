---
- name: Configure Turing Pi Cluster
  hosts: turing_cluster
  become: true


  tasks:
    - name: Ensure htop is installed
      ansible.builtin.apt:
        name: htop
        state: present

    - name: Ensure cluster info directory exists
      ansible.builtin.file:
        path: /opt/turing-cluster
        state: directory
        mode: "0755"